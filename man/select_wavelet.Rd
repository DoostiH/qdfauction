% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wavelet.R
\name{select_wavelet}
\alias{select_wavelet}
\title{Cross-validated choice of (j0, h) for the wavelet estimator}
\usage{
select_wavelet(
  x,
  j0_grid = 3:6,
  h_grid = c(0.03, 0.05, 0.08, 0.12, 0.2, 0.3),
  folds = 5L,
  ...
)
}
\arguments{
\item{x}{numeric sample.}

\item{j0_grid, h_grid}{candidate values.}

\item{folds}{number of folds.}

\item{...}{passed to \code{\link[=qdf_wavelet]{qdf_wavelet()}} (e.g. \code{threshold}, \code{smooth}, \code{filter}).}
}
\value{
a list with \code{j0}, \code{h}, and the matrix of criterion values
(\code{ll}, rows \code{j0_grid}, columns \code{h_grid}).
}
\description{
Chooses the resolution level and the post-smoothing bandwidth of
\code{\link[=qdf_wavelet]{qdf_wavelet()}} by \code{folds}-fold cross-validated held-out log-likelihood: the
implied density \eqn{\hat f(x) = 1/\hat g(\hat F_{\rm train}(x))} is scored on the
held-out observations. This is the criterion used for the same estimators in
the first-price auction application (Doosti, Dewan and Sbai 2026).
}
\examples{
set.seed(2)
x <- rbeta(300, 0.5, 0.5)
select_wavelet(x, j0_grid = 4:6, h_grid = c(0.05, 0.15), folds = 3)
}
